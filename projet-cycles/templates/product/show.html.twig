{% extends 'base.html.twig' %}

{% block title %}{{ product.name }}{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-5">
            <img src="/uploads/{{ product.illustration  }}" class="img-fluid">

        </div>
        <div class="col-md-7 my-auto">
            <h1>{{ product.name }}</h1>

           <p>{{ product.description }}</p>
               <hr>
        <div class="product-price">
            <span><strong>{{ (product.price / 100) | number_format(2,',','.')}} €</strong></span>
        </div>
            <a href="{{ path('add_to_cart', {'id': product.id }) }}"><i class="fa fa-shopping-cart"></i></a>
        </div>

    </div>
{% endblock %}
